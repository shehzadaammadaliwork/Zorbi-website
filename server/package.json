{
  "name": "zorbi-customer-lookup-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal proxy that holds the CRM secret API key server-side for the customer-lookup endpoint used by book-now.html.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0"
  }
}
